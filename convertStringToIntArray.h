// Used to convert user's input into a more easily accessible int array

#ifndef CONVERTSTRINGTOARRAY_H
#define CONVERTSTRINGTOARRAY_H

// Converting string to int array
int* strToIntArray(char* str, int* length);
void printIntegerArray(int integerArray[], int size);

#endif